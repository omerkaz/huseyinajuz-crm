[build]
  command = "npm run build"
  publish = "dist"

# SPA fallback — react-router (createBrowserRouter) handles all routes client-side
[[redirects]]
  from = "/*"
  to = "/index.html"
  status = 200
